// Phase 2 — Module 1: timestep_ctrl
// Hardware equivalent of snnTorch's `for step in range(num_steps)` loop.
// FSM that sequences the per-timestep phases of SNN inference and counts
// timesteps until an inference is complete.
//
// TODO: implement timestep counter and phase-sequencing FSM.

`default_nettype none

module timestep_ctrl ();

endmodule

`default_nettype wire
